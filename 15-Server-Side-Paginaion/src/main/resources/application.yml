server:
  port: 8081
  servlet:
    context-path: /PhoneBook
spring:
  datasource:
    driver-class-name: org.h2.Driver
    password:
    url: jdbc:h2:mem:testdb
    username: sa
  jpa:
    properties:
     hibernate:
      format_sql: true
      show_sql: true
  mvc:
    view:
      prefix: /views/
      suffix: .jsp